import './MainViewDiv.css'
import onEmpty from './onEmpty'
 

function MainViewDiv({ MainView }) {

if (MainView === ''){return(<div><onEmpty /></div>)}
if (MainView === ''){return(<div><onEmpty /></div>)}
if (MainView === ''){return(<div><onEmpty /></div>)}
if (MainView === ''){return(<div><onEmpty /></div>)}

};


export default MainViewDiv